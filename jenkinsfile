pipeline {
    agent any
    stages {
        stage('Container Kill'){
            steps {
                sh "docker rm -f $(docker ps -aq) || true"
            }
        }
        stage('Build'){
            steps {
                sh "docker build -t umayrahmed/jenkinslab3 ."
            }
        }
    }
}
